# ANW Release Program — Autonomous Loop Tracker

This file is the durable brain of the `/loop` autonomous release program. It
survives context compaction. Every loop tick: read this, pick the highest-value
incomplete item, make concrete progress, update this file, schedule next wake.

## North-star goal (user's verbatim asks)
1. Map out the entire game + UI + every nation + every building → wiki.
2. Add to the wiki as we go.
3. Create a validator whenever we solve an issue, to prevent regressions.
   - One line per validator, explaining what it does (see Validators Registry).
4. Get screenshots for every nation; verify they are correct.
5. Improve the AoE custom harness + AI simulator as we go.
6. Speed up every process as much as possible without losing quality.
7. Prefer base-game art; prefer historical portraits; prefer base-game leaders
   + portraits wherever possible.
8. Update the readiness site; use it to verify each nation.
9. Use the AI sim to refine AI behaviour per nation.
10. Use the harness to capture screenshots.

## Tracks
- **T1 Harness reliability** — make capture deterministic + fast (the screenshot
  backend bug, build-tour selection, ESC-menu pause bug). Each fix → a validator.
- **T2 Screenshot coverage** — per-nation surfaces (already ~34 for British).
  Building command-cards are LOW value (shared base-game art) — best-effort only.
- **T3 Wiki / mapping** — game systems, UI surfaces, 44 nations, building rosters.
  Reuse existing: per_civ_building_checklist.md, per_civ_building_capture_map.json.
- **T4 Validators** — codify every solved issue. Registry below. NOTE: 130+
  validators already exist — ALWAYS check `tools/validation/run_all_validators.py`
  before building a new one to avoid duplication.
- **T5 AI sim (xs_sim)** — extend parser to 100% parse coverage, then mock
  kbArea/cAreaType so wall STRATEGY decisions can be simulated per nation.
  GATED: user said don't *run* the sim until all 44 nations signed off in the
  readiness site. Offline parser *development* is allowed.
- **T6 Readiness site** — keep it the single source of truth for per-nation review.

## Hard constraints (from CLAUDE.md + user memory)
- NEVER use cursor-grabbing tools (kwin / Claude Preview / Claude in Chrome).
- Capture only via harness socket SCREENSHOT or gamescopectl (non-intrusive).
- Delegate to cheapest model tier; Opus only coordinates + reviews.
- Do NOT run the AI simulator until user signs off all 44 nations (T5 gate).
- Prefer base-game art / historical portraits / base-game leaders.

## Validators Registry
Each line: `validator_path` — what it checks (one sentence). Build under
`tools/validation/`. Keep this list append-only.

- `tools/validation/validate_capture_backend.py` — flags any STANDALONE-runnable
  `tools/aoe3_automation/*.py` (has an `if __name__=="__main__"` guard) that calls bare
  `_screenshot_raw()` while importing the game drivers but never calls set_harness_backend();
  instance `.screenshot()`, local `snap()` wrappers, and library-only modules (no entry
  point, backend registered by their parent runner) are correctly skipped. Currently GREEN.
- `tools/validation/validate_build_tour_cancel.py` — asserts anw_building_tour.py uses
  _rclick placement-cancel (verified to NOT open ESC menu) and does NOT contain _key("Escape")
  ESC-menu pause bug. Currently GREEN.
- `tools/validation/validate_anw_wall_strategy_coverage.py` — asserts every ANW civ token from
  data/civmods.xml (44) has an EXPLICIT wall-strategy assignment in aiWallKnobsByCiv.xs or
  leaderCommon.xs, exiting non-zero and naming any token that would silently fall through to the
  FortressRing (strategy 0) default. Currently GREEN (44/44 covered).
- `tools/validation/validate_xs_sim_parse_coverage.py` — parses all 66 game .xs files under
  game/ai/ via the xs_sim parser and asserts coverage >= baseline 66, exiting non-zero and listing
  any file that fails to parse (locks in 100% parse coverage against parser/.xs regressions).
  Currently GREEN (66/66).
- `tools/validation/validate_xs_sim_interpreter_smoke.py` — asserts all 17 new
  xsVector*/kbArea*/kbBase* builtins are registered, the wall-strategy code
  (anwDetectChokepointVector, anwDetectCoastVector) executes without exception on canned GameState
  fixtures, and chokepoint detection returns a NON-fallback vector on a chokepoint map (proves the
  interpreter can run the AI wall logic deterministically). Currently GREEN.
- `tools/validation/validate_no_probe_chat_leak.py` — asserts the three control flags (cANWProbeToHostChat, cANWProbeToSelfChat, cANWDebugVisible) are declared false in aiGlobals.xs, and that every aiChat() call inside anwProbe() in aiUtilities.xs is guarded by an if-flag while aiEcho() stays unconditional, so AI diagnostic probes can never leak into player-visible chat.
- `tools/validation/validate_civ_picker_map.py` — asserts every <main>1</main> civ token in civmods.xml has a picker-map entry, no two civs share a picker index, and all indices are in range; guards the screenshot-capture civ-selection from the off-by-one/duplicate-index bug that picked the wrong civ for 5 civs.
- `tools/validation/validate_no_dead_prefixes.py` — fails if any validator or AI-design tool greps for a dead pre-rename identifier (gLL/llXxx/cLL/[LLP/llProbe); prevents the stale-pattern bug where a validator silently misreads the renamed gANW/anw XS and returns a false result (caught after 3 validators were found stale).
- `tools/validation/validate_leader_quote_integrity.py` — checks every ANW leader has non-empty insult+compliment quotes in aiLeaderQuotes.xs with no placeholder/TODO text, no non-ASCII characters (chat can't render them), no unescaped quote breaking the XS string, and no duplicate quote shared across leaders.
- `tools/validation/test_wall_closure_parser.py` — unittest self-test that feeds a synthetic [ANWP] log through validate_doctrine_compliance.parse_probes + evaluate_wall_closure and asserts the smart-wall closure watchdog returns the right PASS / FAIL / SKIP / INCOMPLETE verdict per civ; also pins the parse_probes() return-shape contract (it silently broke when parse_probes grew from a 2-tuple to a 3-tuple, leaving the test collecting 0 tests + erroring). Gated as `self_wall_closure_parser`.
- `tools/validation/validate_revolt_disabled.py` — static guard that mid-game revolting stays fully removed at all 3 layers: TECH (revolt-trigger techs unobtainable by default, with an allowlist for passive revolution-*named* non-trigger techs), DATA (LLDisableStockRevolutionsGlobal exists + is obtainable), AI (gANWAllowRevolt declared false in aiGlobals.xs + the aiTechs.xs revolt decision guarded by it). Currently PASS.
- `tools/validation/validate_revolution_doctrine.py` — static validator that for every civ where civIsRevolution()==true (minus dedicated-leader-file civs) asserts anwInitRevolutionCommander() has a matching `else if (rvltName=="C")` branch, catching the bug class where a revolution civ is recognised but has NO commander doctrine wired (would fall back to generic AI, losing its historical playstyle). Currently PASS (1 harmless warn: ANWYucatan dangling recognizer, no shipped lobby civ).
- `tools/validation/validate_wiki_nation_coverage.py` — fails if any active ANW civ token (<main>1</main> in civmods.xml) is missing from the wiki nation/building map at docs/wiki/validation/nations-and-buildings.md, keeping the documentation in sync with the shipped civ list.
- `tools/validation/validate_wiki_wall_strategy_sync.py` — fails if the wiki "Wall Strategy Index" table at docs/wiki/validation/nations-and-buildings.md drifts from the authoritative playstyle_spec.json claims.wall_strategy: asserts every active civ token has a table row, the integer + enum name in each row match the spec, and no stale/extra rows linger. Born from the stale 18-civ table that declared "26 civs TBD" long after every civ already had an explicit wall strategy.
- `tools/validation/validate_wiki_no_stale_placeholders.py` — fails if the nations-and-buildings wiki carries misleading uncertainty-marker phrases ("TBD / needs verification", "not yet validated", "NEEDS VERIFY") that falsely signal an unfinished/unresolved mod claim, and locks the verified fact behind the Asian-Wonder note by asserting data/protomods.xml defines zero Wonder protounits (Asian civ wonders are base-game protounits resolved at runtime, not a mod data gap).
- `tools/validation/validate_anw_civ_token_prefix.py` — fails if any active (<main>1</main>) civ token in data/civmods.xml lacks the load-bearing `ANW` prefix; the prefixed token is the primary key joined across homecity XML / personality / decks / playercolors, so a civ added (or a base civ left) at main=1 without the prefix would collide with a base civ or dangle every cross-reference. Born from a wiki-accuracy drift where civmods.md's worked example showed a bare `British` token at main=1 instead of the real ANWBritish.
- `tools/validation/validate_wiki_datalayer_accuracy.py` — fails if any data-layer wiki doc (civmods/protomods/techtreemods/stringmods/homecity) omits the REAL root element of the data file it documents, or maps to a non-existent data path; catches the recurring drift where a schema example used the base-game root (`<proto>`/`<techtree>`/`<stringtable>`) instead of the ANW additive root (`<protomods>`/`<techtreemods>`/`<stringmods>`), which would teach a copy-paste that silently fails to merge.
- `tools/validation/validate_wiki_tool_links.py` — walks every markdown doc under `docs/wiki/` and asserts each referenced `tools/<...>.py` script AND each `artifacts/<...>` evidence file (link targets and inline code in "## Tools" tables, "Sources", and cross-references) resolves to a real path on disk; catches the link-rot that happens when a file is renamed, moved, or regenerated (e.g. a phantom `tools/aoe3_automation/scenario_emitter.py` when the real one lives under `tools/validation/scenario_emitter.py`), so the wiki never points a reader at a dead path. `data/*.xml` is intentionally excluded (it has legitimate "does NOT ship" mentions) and is covered by `validate_wiki_phantom_data_files.py`.
- `tools/validation/validate_wiki_phantom_data_files.py` — asserts known non-existent data-overlay files (`data/stringmods.xml` — the real one is per-locale at `data/strings/<lang>/stringmods.xml`; plus the never-shipped `data/protoymods.xml` / `data/techtreeymods.xml` / `data/civmodsy.xml`) never appear inside a fenced code block in the wiki, where they'd read as real copy-pasteable paths; inline prose may still correctly say the file does NOT ship. Also self-checks that the phantom set is still phantom on disk so it can't go stale. Born from `additive-data-mods.md` once listing those phantoms in a code block.
- `tools/validation/validate_wiki_civ_count.py` — pins every TOTAL-roster "N ANW civ tokens" / "all N civs" claim in the wiki to the live active-civ count (number of `<main>1</main>` blocks in `data/civmods.xml`, currently 44); narrow on purpose so it does NOT match per-culture sub-group counts (10 European, 19 Asian, … that sum to 44) or the 66 total `<civ>` blocks. Born from three docs carrying a stale "restored 46 ANW civs" note that contradicted the verifiable 44 (git showed the roster grew 40→44 and was never 46). Any future roster change forces every total-count claim to update in lockstep, in both directions.
- `tools/validation/validate_wiki_culture_rosters.py` — asserts the per-culture civ breakdown in `nations-and-buildings.md` is internally consistent and partitions the live roster: each "N ANW civs: a, b, c" line's stated count equals the civs it lists, no civ is filed under two cultures, and the union of all listed civs equals exactly the set of active `<main>1</main>` tokens in `data/civmods.xml` (so the 12 per-culture counts always sum to the live total and a data-side add/remove can't silently desync the breakdown). Complements `validate_wiki_nation_coverage.py`, which only checks each token APPEARS.
- `tools/validation/validate_wiki_building_protos.py` — asserts every building proto named in a roster-table row of `nations-and-buildings.md` (the backticked middle column, e.g. `TownCenter`, `WarHut`, `FortFrontier`, `YPBarracksIndian`) resolves to a real proto in the extracted authoritative proto-name map (`artifacts/unit_index/full_proto_name_to_locid.json`, ~2456 names), so a typo or renamed/fabricated building can't slip into the roster docs; scoped to table rows so it ignores tech/effect/civ tokens in prose.
- `tools/validation/validate_visual_capture_integrity.py` — asserts every per-civ screenshot dir under `artifacts/validation/visual_art/<civ>/` is intact and correctly attributed: (1) the manifest's `civ_token` equals the directory name (catches a capture filed under the WRONG civ — a French screenshot in `ANWSpanish/`); (2) the reliable floor surfaces `full/01_lobby.png` + `full/02_loading.png` exist for every active civ (catches a run that silently produced nothing); (3) every full-res PNG loads as a real image and is not a black/blank frame (mean luminance ≥ 8, catching the gamescope race where a screenshot fires before the frame renders). Pins the documented capture ceiling (AoE3 crashes mid-skirmish for many civs) as the floor so any future capture regression turns the gate red. PASS 44 dirs / 158 PNGs. Gated as `visual_capture_integrity`.
- `tools/validation/validate_harness_rewire_after_relaunch.py` — asserts the visual-capture runner re-wires the harness control socket after every game relaunch. The runner clicks through the AOE3DEHarness socket wired once at startup; when a civ run relaunches AoE3 (`--force-relaunch-between-civs` or the `aoe3_dead_pre_civ` path) the old socket belongs to the dead process, so every later click raises `[Errno 32] Broken pipe` and the civ fails at `click_skirmish` (reproduced 2026-06-09: a force-relaunch run silently lost 3 of 4 civs). Checks every gating relaunch site (`if not _relaunch_aoe3()`) is followed within 15 lines by a `_wire_harness()` re-wire, so the fix can't be dropped by a future edit. Gated as `harness_rewire_after_relaunch`.
- `tools/validation/validate_no_loading_screen_contamination.py` — classifies every captured full/ screenshot with the same detector the runner uses (image_utils.is_splash_frame, which now covers BOTH the bright-globe splash AND the dark 'Asset Preloading (Beta)' screen) and fails if any surface that should be a real game/UI frame is actually a loading screen. Closes the gap the non-black luminance check left open — a loading screen is non-black (mean_lum ~38) so visual_capture_integrity passed it.

## Solved issues (→ each must yield a validator)
- **2026-06-08 screenshot-backend bug**: `anw_building_tour.py main()` never
  called `set_harness_backend()`, so `_screenshot_raw()` fell back to the broken
  xdotool-PrintScreen path → every capture returned FAILED. Fixed by registering
  the harness client. Validator: validate_capture_backend.py.
- **2026-06-08 build-tour ESC pause bug (FIXED)**: the "clear placement ghost"
  unconditional `_key("Escape")` opened the ESC menu and PAUSED the game, derailing the run.
  Fixed by replacing with `_rclick(*spot, delay=0.3)` (right-click cancels placement ghost
  without opening ESC menu). Validator: validate_build_tour_cancel.py.
- **2026-06-08 wall-strategy fall-through bug (FIXED)**: `kbGetCivName` returns the ANW token
  (e.g. "ANWBritish") but `game/ai/core/aiWallKnobsByCiv.xs` only matched base tokens ("British"),
  so 18 of 44 ANW civs silently fell through to `gANWWallStrategy = 0` (FortressRing) instead of
  their intended strategy. Fixed by adding 18 explicit entries to `tools/ai_design/wall_knob_calibration.py`
  CALIBRATION (Coastal=British/Dutch/Portuguese; Urban=Germans/Italians/Mexicans/USA;
  Mobile=Haudenosaunee/Japanese/Lakota/Spanish/Swedes; Frontier=Hausa; +5 explicit Fortress) — knob
  values copied from base-civ counterparts — then re-emitting aiWallKnobsByCiv.xs (now 62 branches).
  Also fixed stale `gLLWall*`→`gANWWall*` / `llSetWallKnobsForCiv`→`anwSetWallKnobsForCiv` in the
  emitter + validate_per_civ_wall_knobs.py. Validator: validate_anw_wall_strategy_coverage.py.
  NOTE residual: 4 new state civs (ANWCalifornians, ANWCentralAmericans, ANWBajaCalifornians,
  ANWRioGrande) still fail validate_per_civ_wall_knobs.py — pre-existing playstyle_spec.json gap,
  unrelated to this fix; tracked as a Next target.
- **2026-06-08 build-tour single-building bug** (OPEN): villager re-selection
  goes stale after the first settler walks off, so only the first placement
  builds; no structure is ever selected → no command card is ever captured.

## Key facts (from .llm/release_program/inventory.md — read it for detail)
- Wiki EXISTS at `docs/wiki/` (AI, data, UI, validation, file formats). ADD to it.
- 130+ validators exist; readiness site renders 10 art surfaces + 8 screenshot cols.
- Screenshots: 40/43 civs have 7–9 baseline PNGs; British has 54. 2,430 PNGs total.
- Building maps: 12 cultures resolved across the civs.
- Harness speed-ups: screenshot batching, socket reuse, path caching, hot-reload
  cache, bisect memoization. xs_sim: parse caching, parallel leader exec, rule batch.

## Next targets (highest value first)
1. **Missing screenshots: 4 STATE civs have ZERO PNGs / no art folders** (CORRECTED
   2026-06-08 by readiness audit — previously listed only 2): ANWBajaCalifornians,
   ANWCalifornians, ANWCentralAmericans, ANWRioGrande. These 4 are the SOLE blocker for
   44/44 review-ready (40/44 are review-ready now). Need LIVE capture via harness/gamescopectl.
2. Fix build-tour ESC-pause + stale-villager bugs (T1) so building-tour can run.
3. Add a wiki mapping page under `docs/wiki/` for UI surfaces + per-nation building
   rosters (reuse per_civ_building_capture_map.json). Each solved bug → validator.
   [DONE: docs/wiki/validation/ui-surfaces.md + nations-and-buildings.md created.]
3b. [DONE] 4 state civs (ANWCalifornians/CentralAmericans/BajaCalifornians/RioGrande)
   that failed validate_per_civ_wall_knobs.py were a validator false-positive — spec
   entries existed + correct; the CALIB_TO_SPEC map just lacked the 4 tokens. Added 4
   mapping lines → 62/62 PASS. (BajaCalifornians + RioGrande still have ZERO PNGs — that
   remains under target #1, blocked on live-capture reliability.)
4. [DONE] capture-backend offenders cleared: exhibition_runner_anw.py fixed;
   av_probe.py is library-only (correctly skipped). Validator GREEN.

## Per-tick log
- Tick 1 (2026-06-08): discovered + fixed screenshot-backend bug live; identified
  ESC-pause + stale-villager bugs in anw_building_tour.py. Building command-cards
  declared best-effort (shared art). Built capture-backend validator (then tightened
  it: only bare `_screenshot_raw()` w/o registration is flagged → 2 real offenders:
  av_probe.py, exhibition_runner_anw.py). Ran repo inventory groundwork → wiki at
  docs/wiki, 130+ validators, RioGrande+BajaCalifornians missing all screenshots.
- Tick 2 (2026-06-08): applied BUG 1 fix (ESC-pause). Added _rclick import to
  anw_building_tour.py:56-63. Replaced _key("Escape") placement-cancel with
  _rclick(*spot, delay=0.3) at line 201-203 (now 201-205). Built validate_build_tour_cancel.py
  validator. Both validators now GREEN (validate_build_tour_cancel.py + validate_capture_backend.py).
- Tick 3 (2026-06-08): offline AI-behaviour fix (T5 analysis allowed; no sim run). Audited
  per-civ wall-strategy coverage → found 18/44 ANW civs silently fell through to FortressRing
  (kbGetCivName returns ANW token, aiWallKnobsByCiv.xs only matched base tokens). Fixed via
  wall_knob_calibration.py + re-emitted aiWallKnobsByCiv.xs (62 branches). Built
  validate_anw_wall_strategy_coverage.py (44/44 GREEN). All 4 validators GREEN
  (coverage + xs-syntax + build-tour-cancel + capture-backend). Surfaced residual: 4 state civs
  missing from playstyle_spec.json. Findings doc: .llm/release_program/wall_strategy_gap.md.
- Tick 4 (2026-06-08): closed the wall-strategy track. The 4 state-civ failures in
  validate_per_civ_wall_knobs.py were a validator false-positive (CALIB_TO_SPEC map stale,
  spec entries already correct) — added 4 mapping lines → 62/62 PASS, exit 0. Wall track now
  fully GREEN: coverage 44/44 + per-civ knobs 62/62 + xs-syntax. No new validator (the existing
  validate_per_civ_wall_knobs.py self-guards by failing loudly on a stale map — which is how this
  was caught). Findings doc: .llm/release_program/state_civs_playstyle_gap.md.
- Tick 5 (2026-06-08): T5 offline parser dev (no sim run). Measured xs_sim parse coverage
  (51/66 = 77.3%) → fixed ALL 8 ranked parser gaps + 2 bonus (lambda/fn-ptr literals, bare
  `include`, `mutable`, bitshift, `class` decls + member access, empty `return ()`, `debugTechs`,
  fn-ptr params; bonus: case-insled rule attrs, compound-assign ops) in tools/xs_sim/{lexer,parser,
  ast_nodes}.py → 66/66 = 100%, ZERO regressions on the original 51. Built
  validate_xs_sim_parse_coverage.py (baseline 66, GREEN). Verified by me: validator exit 0 + imports
  OK. Ran tools/xs_sim/tests: 20 pass / 3 fail — the 3 fails are PRE-EXISTING smart-walls content
  assertions in aiBuildingsWalls.xs (missing wall.chokepoint/wall.coast "invalidated" cache-miss
  probes), NOT parser-related (git confirms aiBuildingsWalls.xs untouched this session). Findings:
  .llm/release_program/xs_sim_parse_coverage.md. → next tick: close those 3 smart-walls probe tests.
- Tick 6 (2026-06-08): ATTEMPTED smart-walls invalidation-probe fix (add wall.chokepoint/wall.coast
  "invalidated" llProbe lines to aiBuildingsWalls.xs for the 3 failing test_smoke tests). Agent hit
  a USAGE LIMIT ("out of extra usage, resets 11pm Australia/Sydney") after 20 tool calls and made NO
  edit — git confirms aiBuildingsWalls.xs UNCHANGED, repo safe (parse 66/66, tests still 20p/3f). No
  progress this tick. RATE-LIMITED until ~11pm Sydney.

## OVERNIGHT PLAN (2026-06-08 night, usage-constrained) — user wants AI sim + harness + readiness
## site + nations "perfect & ready for review" by morning. HONEST blockers + queue:
- HARD BLOCKERS (cannot do autonomously tonight):
  * AI-sim RUNNING is GATED by user's own rule (no sim run until 44 nations signed off in readiness
    site). Only OFFLINE parser dev allowed — already at 100% parse coverage. Do NOT run the sim.
  * New nation SCREENSHOTS need a LIVE game + the build-tour, which is broken (stale-villager bug,
    needs live timing probe) and cursor-grab is forbidden. Cannot capture RioGrande/BajaCalifornians
    autonomously. Surface to user.
- OFFLINE-SAFE QUEUE (pace ~hourly so ticks land after the 11pm Sydney usage reset; cheapest model
  per task; Opus only coordinates):
  1. [Sonnet] Smart-walls invalidation probes → close the 3 failing test_smoke tests (retry of Tick 6).
  2. [Haiku] Regenerate / refresh the readiness site offline (tools/validation/build_release_readiness_site.py)
     so it reflects current state; verify it builds. → validator if a build issue is fixed.
  3. [Sonnet] Fill wiki TBDs (Wonder protos Chinese/Indian/Japanese; Barbary Mosque; Swedish Torp;
     Mayan Hacienda) in docs/wiki/validation/nations-and-buildings.md (verify against data, no guesses).
  4. [Sonnet] xs_sim: now parse=100%, extend INTERPRETER/builtins coverage offline (still no sim run
     vs live AI) — measure which builtins game .xs needs but are unimplemented; groundwork doc.
- EACH SOLVED ISSUE → one new validator line in the registry above (user's explicit standing rule).
- If a tick is rate-limited: re-arm at 3600s and bail in ONE sentence (don't burn Opus narrating).
- Tick 7 (2026-06-08): readiness-site agent → site BUILDS CLEAN (exit 0, 68/68 validators),
  all 44 nations render w/ corrected wall strategy + art + screenshot cols. 40/44 fully
  review-ready; 4/44 blocked = the 4 STATE civs (zero screenshots). Wrote 44-row gap table
  .llm/release_program/readiness_review_gaps.md. Non-blocking uniform gap: 4 art surfaces
  (home_city_button, hud_flag_corner, esc_menu_player_summary, tech_tree_overview) missing from
  40 manifests — site shows them as MISSING cells w/ recapture cmds. Suggested validator
  validate_no_zero_screenshot_civs.py (assert every civmods.xml civ has >=1 PNG) — DEFER until
  after capture so it lands GREEN, else it'd break the 68/68 suite.
- USER UPDATE (2026-06-08 late): user AUTHORIZED launching the game + testing a full sim with
  base-game nations. Live capture of the 4 state civs is now in-bounds (harness/gamescopectl ONLY,
  never cursor). xs_sim "full sim" interpreted as base-game AI .xs end-to-end through the interpreter
  (deterministic) — implementation agent ab16990e running. Live-capture agent launched for 4 state civs.
- Tick 8 (2026-06-08): xs_sim INTERPRETER hardened (T5 milestone). Implemented 17 builtins
  (8 xsVector*, 6 kbArea*, 3 kbBase*) + AreaInfo dataclass/areas+bases dicts/scenario builders in
  gamestate.py + 5 cAreaType*/cInvalidVector consts + tuple-aware vector arithmetic in interpreter
  _binop. FULL SIM TEST with a BASE-GAME leader (Napoleon): wall-strategy code runs end-to-end,
  anwDetectChokepointVector→(130,0,60) real vector, anwDetectCoastVector→(100,0,88.6) real (inland-
  pushed) — NOT fallbacks. Built validate_xs_sim_interpreter_smoke.py. Verified by me: smoke
  validator exit 0, parse 66/66, tests 20p/3f (3 pre-existing smart-walls fails, no regressions).
  The sim can now deterministically execute AI wall logic. Remaining: ~373 lower-freq builtins still
  warn-once+zero (safe stubs); full per-leader behaviour sim needs more builtins but wall path solid.
- Tick 9 (2026-06-08): Fixed the AI-probe-in-chat leak the user reported. Root cause: anwProbe() in game/ai/core/aiUtilities.xs had an UNCONDITIONAL aiChat(cMyID, line) call (was line 294) that printed every [ANWP v=2 ...] diagnostic probe into the AI's own chat, burying personality quotes. Fix: added new flag `extern const bool cANWProbeToSelfChat = false;` in aiGlobals.xs (line 587) and gated the aiChat(cMyID,line) call behind it (aiUtilities.xs lines 294-297); aiEcho(line) stays unconditional so the validator/log capture channel is unaffected. Verified: validate_no_probe_chat_leak.py exit 0, validate_xs_sim_parse_coverage.py exit 0 (66/66 maintained). New validator validate_no_probe_chat_leak.py added to registry.
- Tick 10 (2026-06-08): Surfaced + fixed a 14-validator readiness regression that was mostly STALE VALIDATORS, not mod defects. Root cause: the gLL->gANW codebase rename left 3 tooling scripts grepping dead prefixes — validate_spec_vs_xs_doctrine.py (gLLForwardBaseEarliestMs/llEnableEarlyForwardBase), validate_probe_tag_drift.py (llProbe -> anwProbe, was reporting 14/14 tags emitterless), and ai_behaviour_map.py (12 gLL/ll/cLL patterns, derived 0 build styles -> 40 false wall_strategy mismatches). All updated to gANW/anw/cANW; the underlying mod data + AI behavior were correct (verified: Shivaji expects_forward=True and Romanians-Cuza=False both match HTML design + actual XS). Separately, activating the 4 state civs (ANWBajaCalifornians/Californians/CentralAmericans/RioGrande) in the lobby picker surfaced real data gaps now fixed: player colors (playercolors.xml), blurbs (anw_civ_blurbs.json + age_build_notes.json), post-game flag refs (allowlisted like the 10 existing revolution civs), spec invariants (Baja=expects_naval, Californians wall deadline=720000ms), and hub_test_roster regenerated to 44 civs/7 passes. Added validate_no_dead_prefixes.py meta-validator. Remaining known-FAIL (pre-existing, not tonight): xs_sim_doctrine (4 civs cavalry-bias tuning), hub_test_probe_coverage (3 milestone narrative tokens in anwHubTest.xs lack emitters), live_mod_install (live Proton install stale vs tonight's .xs edits, clears on sync).
- Tick 11 (2026-06-08): AI doctrine refinement + finished the stale-validator sweep. (1) Combat-bias gap: 4 revolution commanders claimed a unit-type emphasis in playstyle_spec.json that their XS never implemented, so the AI ignored their historical doctrine. Fixed in game/ai/leaders/leader_revolution_commanders.xs by adding direct btBias{Inf,Cav,Art} assignments (the mechanism the xs_sim interpreter models + 18 passing civs use): ANWColumbians/Bolivar btBiasCav=0.7 (llaneros cavalry), ANWEgyptians/Muhammad Ali btBiasArt=0.6 (modern artillery corps), ANWHaitians/Louverture btBiasInf=0.85 (infantry armies), ANWNapoleonicFrance/Napoleon btBiasCav=0.6 (combined-arms cavalry). validate_xs_sim_doctrine.py now 22 PASS/0 FAIL; parse-coverage 66/66 and spec_vs_xs_doctrine stay green. (2) The no_dead_prefixes meta-validator surfaced 4 MORE stale tooling scripts beyond the original 3 and they were fixed too: audit_engine_vs_spec.py, validate_leader_vs_spec.py (was silently parsing 0 style helpers -> now 17/17), validate_hub_test_probe_coverage.py (llCheckMilestone->anwCheckMilestone), validate_playstyle_modal.py. Full gate now 69/71->70/71 (only live_mod_install deploy-lag remained, synced separately). Net: the overnight 'regression' was 7 stale validators from the gLL->gANW rename plus real data gaps in the 4 newly-activated state civs, not mod defects.
- Tick 12 (2026-06-08): Quote refinement + wiki-sync guard. Refined leader quotes for 30 leaders (27 refine + 3 replace) in aiLeaderQuotes.xs/chatsetsmods.xml to be historical + mod-matching; added validate_leader_quote_integrity.py. CAUGHT a leader mis-attribution: the quote pass guessed the British leader as Wellington, but the mod's canonical British leader is Elizabeth I (confirmed via data/doctrine_specs/british_elizabeth.yaml leader_label:Elizabeth + cpai_avatar_british_elizabeth.png + the Elizabeth blurb history) — corrected all British quotes back to Elizabeth I across aiLeaderQuotes.xs + chatsetsmods.xml, zero Wellington refs remain (the lone 'Fort Wellington' in the Canadian chatset is the Ontario fort, correct). Added validate_wiki_nation_coverage.py to lock the wiki nation/building map against drift from the civ list. Re-synced live install for the quote edits. FINAL GATE: 72/72 PASS, 0 fail; readiness site rebuilt (review-ready). 5 validators added this session total: no_probe_chat_leak, civ_picker_map, no_dead_prefixes, leader_quote_integrity, wiki_nation_coverage.

- Tick 13 (2026-06-09): Closed two SILENT validator gaps in the smart-walls track (the Track-1 smart-wall AI itself — anwDetectChokepointVector, anwDetectCoastVector, anwSelectWallType, water/cliff avoidance, and the verifyWallClosure watchdog with wall.chokepoint/wall.water_fix/wall.closure probes — was already fully implemented in a prior session; XS syntax clean). (1) `validate_anw_wall_strategy_coverage.py` (PASS, 44/44 civs covered) existed + was in the registry but was NEVER registered in run_all_validators.py, so it never ran in the gate or showed on the readiness site — now gated as `anw_wall_strategy_coverage`. (2) `test_wall_closure_parser.py` was BROKEN: it unpacked `parse_probes()` into 2 values but validate_doctrine_compliance.parse_probes now returns a 3-tuple (probes, plan_closures, flat_closures), so it collected 0 tests and errored (a self-test that silently stopped testing). Fixed all 3 unpack sites (lines 111/183/204) to discard flat_closures; now 4/4 tests pass; gated as `self_wall_closure_parser`. Fixed the same latent 3-tuple-unpack bug in compile_playtest_report.py:255 (was assigning the whole tuple to `probes` then iterating it as a probe list — would AttributeError on any live match.log). Gate after step (2): 75/75 PASS. (3) Swept the full registry-vs-gate drift: 4 MORE validators that PROGRAM.md documents as GREEN had never been registered in run_all_validators.py, so they ran nowhere — `validate_xs_sim_parse_coverage.py` (66/66 parse coverage), `validate_xs_sim_interpreter_smoke.py` (smart-wall interpreter executes deterministically), `validate_capture_backend.py` (no screenshot tool captures with an unregistered backend), `validate_build_tour_cancel.py` (building tour cancels via right-click, never ESC). All 4 confirmed PASS + fast (<2s) standalone, then gated. NOTE: ~18 other validate_*/test_* scripts on disk are NOT gated on purpose — legacy/superseded (tier1/2/3, per_age_v2, six_civ_playstyle, etc.); registering them blind would redden the gate, so they stay out until individually vetted. Gate after step (3): 79/79 PASS. (4) Triaged ALL ~18 remaining ungated validate_*/test_* scripts by running each: most are CLI tools needing args (tier3_gameplay/civ_behavior/scenario_binary), game-dependent (engine_merged_xml SKIPs without DebugOutputGameData; art_visual/tier2_scenario need capture artifacts), or report-mode tools that exit 0 / depend on stale telemetry despite internal failures (ai_playstyle mismatched=4, per_age_v2 0/7, tier3_comparison 0/11) — all correctly left ungated to avoid misleading green rows or reddening the gate. Found 2 MORE clean, static, non-redundant invariants worth gating: `validate_revolt_disabled.py` (revolting fully removed at tech+data+AI layers) and `validate_revolution_doctrine.py` (every civIsRevolution() civ has a wired commander-doctrine branch). NOTE: validate_personality_vs_spec + validate_six_civ_playstyle FAIL on sim/match artifacts (e.g. Californians/RioGrande wall_strategy FAIL at match_ms=60000 — match too short to wall; not a mod defect); their AI-behaviour coverage is already gated green by xs_sim_doctrine (22/0) + per_civ_wall_knobs + doctrine_compliance, so they stay ungated. HELD validate_tier1_static (passes but a likely-legacy tier suite overlapping gated civ/deck validators — needs vetting before gating). FINAL GATE: target 81/81 PASS; readiness site rebuilt. 8 validators added to the gate this tick (2 fixed/new + 4 drift-recovered + 2 invariants).

- Tick 14 (2026-06-09): Resolved the DEV-START signal flagged in Tick 13. validate_civmods_art.py + validate_dev_tree.py both error with "no DEV-START blocks found in a_new_world.html". Root cause via git: commit 6db1026 ("Remove dev-tree subsections + Dev Trees Only button from reference site") intentionally stripped the <!-- DEV-START name=".." -->/<!-- DEV-END --> comment markers from the HTML; the only files still carrying them are stale .claude/worktrees copies + 2026-05 artifact runs. So both validators are OBSOLETE-BY-DESIGN, not broken — they validate a removed feature and can never pass (civmods_art raises SystemExit before any work). Confirmed their former coverage is fully retained by GATED validators: dev_subtrees (dev-subtree concept), civmods_art_consistency + civ_asset_existence + art_coverage + civ_crossrefs (HTML/civmods art wiring). Non-destructive fix (no deletion w/o user OK): added a DEPRECATED/DO-NOT-GATE banner to the top of both files citing commit 6db1026, so no future session re-investigates them; both still parse + keep their module docstrings. No gate change (81/81 holds; neither was ever gated).

- Tick 15 (2026-06-09): Fixed a stale wiki documentation defect + added a guard so it can't recur. The "## Wall Strategy Index" table in docs/wiki/validation/nations-and-buildings.md was badly stale — it listed only 18 civ rows and declared "26 civs TBD / not yet validated", long after every active ANW civ had received an explicit wall strategy. Rebuilt the table authoritatively by joining playstyle_spec.json claims.wall_strategy (source of truth, gate-pinned against the AI XS) with data/civmods.xml tokens; cross-checked every value against artifacts/validation/ai_behaviour_map.json (XS-derived) — 0 mismatches across all 44 active civs. Replaced the section with a complete 44-row table + enum legend (FortressRing=0 … MobileNoWalls=5). Per "create validators when you solve issues", added validate_wiki_wall_strategy_sync.py: asserts every active token has a table row, the integer + enum NAME in each row match the spec, and no stale/extra rows linger (regex-scoped to the Wall Strategy Index section; _HARD map handles the two tokens whose data_name diverges from the token stem — ANWRevFrance, ANWUSA). Standalone PASS (44 tokens = 44 spec strategies = 44 wiki rows, all match), gated as `wiki_wall_strategy_sync`. Gate after this step: 82/82 PASS; readiness site rebuilt. THEN fixed a second stale-placeholder defect in the same wiki: the three Asian-civ building tables (Chinese/Indian/Japanese) listed "Wonder (age-up) | TBD / needs verification" with an "OPEN: proto not extracted" note — misleadingly implying an unfinished extraction. Verified the truth: data/protomods.xml defines ZERO Wonder protounits, so the Asian wonders are pure base-game protounits resolved at runtime from the wonder set (YPAge0ChineseWonders / ypAge0IndiansWonders / YPAge0JapaneseWonders) — there is nothing for the mod to list. Rewrote all three notes to state this accurately (proto field is null because there is nothing to override, not because extraction is incomplete). Added validate_wiki_no_stale_placeholders.py: fails on any "TBD / needs verification" / "not yet validated" stale phrase in the wiki AND asserts protomods.xml still defines no Wonder proto (so the note can't go stale if a future change adds one). Standalone PASS, gated as `wiki_no_stale_placeholders`. Gate after this step: 83/83 PASS; readiness site rebuilt. THEN resolved the last uncertainty markers in the same wiki: the Per-Civ Building Overrides table carried three "(NEEDS VERIFY)" rows. Verified all three from data/techtreemods.xml: (a) ANWBarbary Church→Mosque CONFIRMED — `ANWAge0Barbary` activates `DECivHasMosque` (~l.5522), same path as Ottomans/Egyptians; (b) ANWMayans +Hacienda CONFIRMED — `ANWAge0Maya` (l.6194) has an explicit `Enable` effect on `deHacienda` (~l.6461) plus WarHut enables, so it's start-of-game, not HC-card-only; (c) ANWSwedes +Torp CONFIRMED — base-game Swedish signature building via `DEAge0Swedish` (unchanged by mod), corroborated by the sibling `ANWAge0Finnish` chain enabling `deTorp` at l.4072. Rewrote all three rows with the verified citations; strengthened validate_wiki_no_stale_placeholders.py to also catch the "NEEDS VERIFY" marker class so these can't regress. FINAL GATE: 83/83 PASS (no new validator — existing one strengthened in place); readiness site rebuilt. 2 validators added this tick (wiki_wall_strategy_sync, wiki_no_stale_placeholders).

- Tick 16 (2026-06-09): Continued the wiki-accuracy audit into the data-layer docs. (1) Added a "Wiki / documentation sync" category to docs/wiki/validation/static-gate.md documenting the new doc-consistency validator class (wiki_nation_coverage, wiki_wall_strategy_sync, wiki_no_stale_placeholders, anw_wall_strategy_coverage) and bumped the intro count "~80"→"80+"; verified all 4 linked validator files exist. (2) Audited docs/wiki/data-layer/civmods.md against the real data/civmods.xml and found its worked XML example had drifted: it showed a bare `<name>British</name>` at <main>1</main> with rollovernameid 410500 and Age0 tech "BritishAge0" — but the ACTUAL active ANW civ is `ANWBritish` (civmods.xml l.5384–5659) with rollovernameid 490201 and Age0 tech "Age0British"; the bare `British` entry is a separate <main>0</main> suppression row (l.15051). Corrected the example to the verified ANWBritish values + added a note explaining the ANW-token-vs-base-suppression distinction. (3) Per "create validators when you solve issues", added validate_anw_civ_token_prefix.py: asserts every active (<main>1</main>) civ token carries the load-bearing `ANW` prefix (verified invariant: 44/44 active tokens are ANW-prefixed, 0 exceptions) — catches a civ added without the prefix or a base civ accidentally left active, either of which would collide with a base civ or dangle the token primary key joined across homecity/personality/decks/playercolors. Standalone PASS, gated as `anw_civ_token_prefix`. FINAL GATE: target 84/84 PASS; readiness site rebuilt. 1 validator added this tick (anw_civ_token_prefix).

- Tick 17 (2026-06-09): Continued the data-layer wiki audit into docs/wiki/data-layer/protomods.md and found significant drift from the real data/protomods.xml. (1) The schema example was rooted at `<proto>` and showed `<unit id="..." name="...">` — but the actual file is rooted at `<protomods>` and ALL units merge by `name` with ZERO `id` attributes (verified: 0 `id=` across the file). (2) The doc presented protomods.xml as a rich unit-stat/cost table, but in reality it is a focused single-purpose overlay: all 58 `<unit>` entries are the SAME minimal override — `<contain mergeMode='remove' ...>AbstractArtillery</contain>` — removing artillery from ship/transport contain lists so artillery can't be garrisoned aboard naval units; there are zero stat/cost overrides (those live in techtreemods.xml). (3) The doc hedged that protomods has a `protoymods.xml` y-variant; verified ANW ships no such file. Corrected the schema root + merge-by-name note, added a "What ANW's data/protomods.xml actually contains" section with the verified example, and fixed the protoymods.xml claim. Per "create validators when you solve issues", STRENGTHENED the already-gated validate_protomods.py (rather than add a redundant new file): it previously only checked duplicate names/ids/dbids; now also asserts (a) the root element is `<protomods>` — a wrong root (e.g. a pasted base `<proto>` block) is silently ignored by the additive merge, and (b) every `<unit>` carries the merge-key `name` attribute — a nameless unit silently fails to merge. Standalone PASS. FINAL GATE: 84/84 PASS (existing validator strengthened in place, no count change); readiness site rebuilt.

- Tick 18 (2026-06-09): Audited two more data-layer wiki docs and found the same root-element drift class as Tick 17, plus a real path error. (1) techtreemods.md was rooted at `<techtree>` (real: `<techtreemods>`, 278 Tech entries verified) and hedged a non-existent `data/techtreeymods.xml`; corrected both. (2) stringmods.md was worse — it claimed the file lives at top-level `data/stringmods.xml` with root `<stringtable>`, but the REAL file is `data/strings/english/stringmods.xml` rooted at `<stringmods>` → `<StringTable>` → `<Language name="English">` → `<String _locID=...>`. Ground-truthed via the gated validate_string_resolution.py (globs data/strings/<lang>/stringmods*.xml) + every repo tool (build_civ_columns.py:129/968, build_dev_reference.py:36) — all treat the per-locale path as authoritative; there is no top-level data/stringmods.xml. Also corrected the ID-band claim: ANW uses 400000+ (leader/civ history blurbs, e.g. 400001 San Martín) AND 490000+ (UI display/rollover names), not just 490000+ as documented. Fixed the path, root, structure, ID band, and Sources line. (3) Per "create validators when you solve issues", added validate_wiki_datalayer_accuracy.py: for each (data-layer doc → data file) pair it parses the data file's REAL root element and asserts the doc names that exact root tag and that the data path exists — covering civmods/protomods/techtreemods/stringmods/homecity (5/5 PASS, roots civmods/protomods/techtreemods/stringmods/homecity). This locks the whole class so no data-layer doc can drift to the base-game root again. Gated as `wiki_datalayer_accuracy`. FINAL GATE: target 85/85 PASS; readiness site rebuilt. 1 validator added this tick (wiki_datalayer_accuracy).

- Tick 19 (2026-06-09): Audited the homecity.md data-layer doc against the real `data/anwhomecitybritish.xml` — root `<homecity>`, all schema fields (`<civ>ANWBritish`, `<name>$$36883$$`, `<heroname>$$490331$$`, visual/water/background/pathdata/camera, level/skillpoints/lightset/watertype, coffers, cards) verified accurate; no drift. Then ran a repo-wide sweep for a NEW drift class: every `tools/<...>.py` path referenced anywhere in `docs/wiki/`. Found two dead references to a phantom `tools/aoe3_automation/scenario_emitter.py` — the real script lives at `tools/validation/scenario_emitter.py` (38 KB, verified). (1) replays-scenarios.md:86 carried a phantom "Partial scenario emitter" row duplicating the real line 87; removed the phantom row. (2) file-formats/l33t.md:28 linked the same dead path; repointed it to `tools/validation/scenario_emitter.py`. Per "create validators when you solve issues", added validate_wiki_tool_links.py: scans all 23 wiki docs, extracts every `tools/*.py` token, and asserts each resolves on disk (101/101 PASS). Gated as `wiki_tool_links`. This locks doc→tool link-rot across the entire wiki, not just the data-layer subset. 1 validator added this tick (wiki_tool_links).

- Tick 20 (2026-06-09): Audited personalities.md and additive-data-mods.md against the real reference files and found two more drift instances of the same base-token / phantom-file class. (1) personalities.md schema example used the base token `<forcedciv>British</forcedciv>` and a wrong `<nameID>490331</nameID>`; the real `game/ai/anwbritish.personality` uses `<forcedciv>ANWBritish</forcedciv>` and `<nameID>490200</nameID>` (with the same ANW token inside `<playerNames>`). Fixed the example and added a field-table note that ANW personalities use the ANW-prefixed token. Confirmed the FUNCTIONAL class is already locked: validate_personality_lobby.py asserts every `<forcedciv>` resolves to a `<name>` in civmods.xml — so no redundant validator was added there. (2) additive-data-mods.md "Overlay file conventions" listed a phantom `data/stringmods.xml`, `data/civmodsy.xml`, `data/techtreeymods.xml`, and `data/protoymods.xml` inside a code fence as if they ship, and omitted the real `data/randomnamemods.xml`. Ground-truthed via `ls data/*mods*.xml`: ANW ships civmods.xml, protomods.xml, techtreemods.xml, randomnamemods.xml — plus stringmods per-locale at data/strings/english/stringmods.xml; none of the `y`-siblings exist. Rewrote the block to show what ANW actually ships and to state the `y`-variants are a DE convention not carried here. Per "create validators when you solve issues", added validate_wiki_phantom_data_files.py: a known-phantom overlay filename must never appear inside a fenced code block (where it reads as a real path), while inline prose may still say it does not ship — the fence/inline distinction cleanly separates the drift from the many legitimate "there is no top-level data/stringmods.xml" negative assertions. It also self-checks the phantom set is still absent on disk. Gated as `wiki_phantom_data_files` (PASS). 1 validator added this tick (wiki_phantom_data_files).

- Tick 21 (2026-06-09): Audited the remaining UI-layer picker docs — select-civilization.md, select-home-city.md, ai-personality-picker.md — against the real data and assets. All field claims match (verified the 5 WPF portrait/flag tags appear once per active civ in civmods.xml in Tick 20; the personality field set matches game/ai/anwbritish.personality), all referenced personality/XS files exist (anwbritish/anwfrench .personality, aiLoaderStandard/aiMain/aiHeader .xs), and all 9 referenced `artifacts/...` evidence files resolve. No content drift found — the docs are accurate. The one gap: `artifacts/...` link targets were an UNGUARDED link-rot category (they only passed by luck, no gate covered them). Rather than add a redundant validator, strengthened validate_wiki_tool_links.py IN PLACE to also extract and resolve every `artifacts/<...>.{xaml,xml,json,html,md,png,txt}` reference alongside the existing `tools/*.py` check (now 110 references across 24 docs, all resolve). Left `data/*.xml` out of this validator on purpose — it has legitimate "does NOT ship" negative mentions and is covered by wiki_phantom_data_files. Net: same gate count (no new spec), broader coverage on an existing one. 0 validators added this tick (one strengthened in place: wiki_tool_links now covers artifacts/).

- Tick 22 (2026-06-09): Found a numeric drift class — three docs (data-layer/civmods.md Sources note, additive-data-mods.md Case-sensitivity section, modding-pitfalls/case-sensitivity.md Verification) claimed the lowercase rewrite "restored 46 ANW civs", but the live count is 44 (44 `<main>1</main>` blocks, 44 ANW `<name>` entries; 66 total `<civ>` blocks incl. suppression). Did NOT guess: checked git history — the active roster grew 40→44 across commits and was never 46 in the `<main>`-era (the 05-09 bug-fix predated the `<main>` structure), so "46" was an unreproducible figure contradicting every other doc (all say 44). Reconciled all three to truthful wording that holds under either history ("the full set of ANW civs — 44 active today"), avoiding asserting an unverifiable historical 46. Per "create validators when you solve issues", added validate_wiki_civ_count.py: pins every TOTAL-roster claim ("N ANW civ tokens" / "all N civs") to the live `<main>1</main>` count. First draft over-matched the per-culture breakdown (10 European / 19 Asian / 6 … which legitimately sum to 44); narrowed the patterns to total-roster phrasings only (require "token" or "all"), now 4 claims scanned, all == 44 PASS. Gated as `wiki_civ_count`. This locks the total-count drift in both directions for any future roster change. 1 validator added this tick (wiki_civ_count).

- Tick 23 (2026-06-09): Hardened the nations-and-buildings.md culture breakdown. wiki_nation_coverage only asserts each active token APPEARS in the doc; it does NOT verify the 12 per-culture roster lines ("10 ANW civs: British, French, …", "1 ANW civ: ANWAztecs.") are self-consistent. Verified by hand the breakdown sums correctly: WesternEurope 10 + Mediterranean 19 + EasternEurope 6 + 9 singleton cultures (Aztec/Chinese/Indians/Japanese/Haudenosaunee/Lakota/Inca/Ethiopians/Hausa) = 44, across 12 cultures. Added validate_wiki_culture_rosters.py: parses each culture line, asserts its stated count equals the civs listed, asserts no civ is double-classified, and asserts the union of all listed civs equals exactly the live active `<main>1</main>` token set in civmods.xml (normalising the doc's mixed bare-stem "British" vs prefixed "ANWAztecs" forms). PASS — 12 lines partition all 44 active civs, zero double/zero/phantom classification. Gated as `wiki_culture_rosters`. This locks the breakdown so a data-side civ add/remove can't silently desync the per-culture map. 1 validator added this tick (wiki_culture_rosters).

- Tick 24 (2026-06-09): Verified the building-proto references in the nations-and-buildings.md culture roster tables resolve to real protos. Extracted the 37 distinct building protos cited in roster-table rows (TownCenter, Barracks, Stable, ArtilleryDepot, Dock, Church, Market, TradingPost, Outpost, FortFrontier, Capitol, WarHut, NoblesHut, CommunityPlaza, Longhouse, Corral, Bank, Manor, Saloon, Blockhouse, YPBarracksIndian, YPDockAsian, …) and checked each against artifacts/unit_index/full_proto_name_to_locid.json (the authoritative extracted proto-name map, 2456 names) — all 37 resolve, 0 missing. No drift. Per "create validators when you solve issues", added validate_wiki_building_protos.py to lock it: parses the backticked middle column of every roster-table row and asserts the proto exists in the map; scoped to `| name | \`Proto\` | age |` rows so it ignores tech tokens (DECivHasMosque), effect subtypes (Enable), and civ tokens (ANWRioGrande) that appear in prose. PASS 37/37. Gated as `wiki_building_protos`. 1 validator added this tick (wiki_building_protos).

- Tick 25 (2026-06-09): "do the in game capture and screenshot and verification." Confirmed game live (AoE3DE_s.exe under gamescope, DISPLAY=:1/gamescope-0), dismissed the blocking weekly Free-Profile-Picture popup via `lobby_driver.dismiss_weekly_popup()` (purpose-built blind-click at known Close coords) → clean main menu re-captured. Then VERIFIED all existing captures rather than trust them: swept all 44 civ dirs under artifacts/validation/visual_art — 122 full-res PNGs, 0 black, 0 corrupt; every manifest civ_token matches its dir; every civ has the reliable floor (full/01_lobby.png + full/02_loading.png). Established the real coverage shape: ANWBritish is the deeply-captured reference (28 in-game surfaces: HUD, scoreboard, diplomacy, homecity, tech tree, age-ups, endgame, AI homecity); the other 40 civs reliably reach lobby+loading(+sometimes ingame); 4 civs (ANWBajaCalifornians, ANWCalifornians, ANWCentralAmericans, ANWRioGrande) crashed before loading — matching the documented AoE3 mid-skirmish crash ceiling. Per "create validators when you solve issues", added validate_visual_capture_integrity.py to lock correctness+attribution+non-black floor; PASS 44 dirs/122 PNGs; gated as `visual_capture_integrity` (gate 90→91, all PASS). Kicked off a live re-capture of the 4 gap civs in host mode with `--force-relaunch-between-civs` (the runner flag documented to give ~100%% success by giving each civ a fresh 1st-iter engine, sidestepping the 2nd-iter crash). 1 validator added this tick (visual_capture_integrity).

- Tick 26 (2026-06-09): Filled the 4 capture gaps AND fixed the harness bug that caused them. The 4 civs stuck at lobby+loading only (ANWRioGrande, ANWCalifornians, ANWBajaCalifornians, ANWCentralAmericans) were re-captured to full coverage (11 surfaces / 10 captures each, 0 warnings) — visual_capture_integrity now PASS 44 dirs / 158 PNGs (+36). Root-caused the partial failure of the first force-relaunch batch (1 of 4 succeeded; 3 failed at click_skirmish with `[Errno 32] Broken pipe`): `_wire_harness()` was called once at startup but never after `_relaunch_aoe3()`, so the persistent harness socket pointed at the dead pre-relaunch process. Fix: re-call `_wire_harness()` immediately after each successful relaunch (both the host `aoe3_dead_pre_civ` path and the ally path), logging `harness_rewired_post_relaunch`; verified AST/compile/validate_capture_backend all green. Per "create validators when you solve issues", added validate_harness_rewire_after_relaunch.py to lock it: asserts every `if not _relaunch_aoe3()` site re-wires within 15 lines. Confirmed the workaround too — running each stuck civ as its OWN process (fresh harness wire vs already-alive game) succeeds 100%%. Readiness site rebuilt (3.63 MB, 44 leader avatars) and confirmed serving HTTP 200 at 127.0.0.1:8765. 1 validator added this tick (harness_rewire_after_relaunch).

- Tick 27 (2026-06-09): Fixed a visual-capture correctness regression and added a validator to prevent recurrence. The HUD_SETTLE_CONFIRMED=3 speed path captured AoE3's second loading screen ("Asset Preloading (Beta)" — a dark frame with a teal progress bar) into ANWBritish/full/03_hud.png and 04_homecity_panel.png, because (a) the splash detector image_utils.is_splash_frame only recognised the bright-globe splash and was blind to the asset-preloading screen, and (b) wait_for_splash_clear in tools/aoe3_automation/anw_visual_capture_runner.py confirmed "clear" off a single t=0 probe before the loading screen had even appeared. Fixes: (1) added is_asset_preloading_frame() to image_utils.py (teal-bar fraction in the bottom strip, threshold 0.020, calibrated 6.8x margin) and folded it into is_splash_frame; (2) rewrote wait_for_splash_clear to only trust a "clear" reading after positively OBSERVING the loading screen clear (seen->gone transition stable for SPLASH_CLEAR_STREAK=2 consecutive probes) or polling past SPLASH_APPEAR_WINDOW_S=27s with no loading screen ever seen; raised SPLASH_CLEAR_TIMEOUT 35->75s; (3) added validator no_loading_screen_contamination. Re-captured ANWBritish (281s) — all 10 surfaces verified correct real game frames; gate validators green (integrity 212 PNGs, contamination 166 surfaces, image_utils selftest 15/15). British capture confirmed PERFECT; all-nations rerun pending user review of the readiness site. 1 validator added this tick (no_loading_screen_contamination).

## Open questions for the user (do not block; surface at a good moment)
- Building command-cards: confirm best-effort/skip (low art value)?
- FrenchCanadians vs LowerCanada naming (prior audit pending).
